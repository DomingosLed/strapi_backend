'use strict';

/**
 * material-derivado service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::material-derivado.material-derivado');
